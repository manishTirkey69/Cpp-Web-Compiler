# Frontend environment variables
# Copy this to .env.local for local overrides.
# Vite only exposes variables prefixed with VITE_ to the browser.

# Override the REST API base URL (default: empty string → relative path /api)
# Example for a remote backend: VITE_API_URL=https://api.myserver.com
VITE_API_URL=
